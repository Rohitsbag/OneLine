# Ambient Audio Directory

Place your custom ambient audio files (e.g. `ambient.mp3`, `rain.mp3`, etc.) in this folder.

By default, Cinematic Mode looks for `/ambient/ambient.mp3`. Drop your favorite background track here!
